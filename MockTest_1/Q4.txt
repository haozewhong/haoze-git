Fill in the missing code inside the Student class so that the program produces the expected output.
You must not modify the main() function.
#include <iostream>
#include <string>
using namespace std;

class Student {
private:
    string name;
    int mark;

public:
    Student(string n, int m) {
        name = n;
        mark = m;
    }

    // Fill in the missing function here

};

int main()
{
    Student s1("Ali", 80);
    Student s2("Maya", 95);

    cout << s1.getName() << " scored " << s1.getMark() << endl;
    cout << s2.getName() << " scored " << s2.getMark() << endl;

    return 0;
}
Expected output:
Ali scored 80
Maya scored 95
Write only the missing function or functions.

string getName(){
    return name;
}
int getMark(){
    return mark;
}